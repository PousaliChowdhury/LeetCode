class Solution(object):
    def isPalindrome(self, x):
        n = x
        rem = 1
        res = 0
        if x < 0: return False
        while n != 0:
            rem = n % 10
            res = res * 10 + rem
            n = n//10
        return res == x
