class Solution(object):
    def findDuplicate(self, nums):
        seen = {}
        for i in range(len(nums)):
            if nums[i] in seen:
                return nums[i]
            seen[nums[i]] = i
        
