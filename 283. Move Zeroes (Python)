class Solution(object):
    def moveZeroes(self, nums):
        n = len(nums)
        pos = 0
        for i in range(n):
            if nums[i] != 0:
                nums[pos] = nums[i]
                pos += 1

        for i in range(pos, n):
            nums[i] = 0

        return nums
