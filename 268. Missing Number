class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        missing = 0
        n = len(nums)
        total_expected_sum = n * (n+1) // 2
        total_sum = sum(nums)
        return total_expected_sum - total_sum

