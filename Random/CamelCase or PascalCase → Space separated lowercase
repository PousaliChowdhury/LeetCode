def format_camel_case(s):
    result = ""
    
    for ch in s:
        if ch.isupper():
            result += " " + ch.lower()
        else:
            result += ch
    
    return result.strip()

if __name__ == "__main__":
    s = input().strip()
    print(format_camel_case(s))
