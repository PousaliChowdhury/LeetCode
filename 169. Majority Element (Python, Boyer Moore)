class Solution(object):
    def majorityElement(self, nums):
        element = None
        count = 0
        for i in nums:
            if count == 0: 
                element = i
            count += 1 if i == element else -1
        return element
        
