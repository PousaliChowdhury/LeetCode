class Solution(object):
    def maxSubArray(self, nums):
        max_sum = nums[0]
        count = nums[0]
        for i in range(1, len(nums)):
            num = nums[i]
            count = max(num, count+num)
            if(count > max_sum):
                max_sum = count
        return max_sum
