class Solution(object):
    def removeDuplicates(self, nums):
        if not nums: return 0
        n = len(nums)
        last = 0
        for i in range(1,n):
            if nums[i] != nums[last]:
                last += 1
                nums[last] = nums[i]
        return last+1
        
